# @package _global_
# Ablation: set visual logging frequency to every step

defaults:
  - /experiments/cw/z002_n1

submitit:
  name: "z002_n1_logvisualfreq_1"

trainer:
  logging:
    log_visual_frequency:
      train: 1
      val: 1



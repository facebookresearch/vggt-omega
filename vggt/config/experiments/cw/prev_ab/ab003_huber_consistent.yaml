# @package _global_
#

defaults:
  - /experiments/cw/ab003_base
  - _self_


trainer:
  loss:
    camera:
      loss_type: "huber_consistent"
      # Huber here is numerically much smaller than L1 at current error scale.
      # Start with ~250x camera task scaling vs ab003_base (10.0 -> 2500.0).
      weight: 2000.0



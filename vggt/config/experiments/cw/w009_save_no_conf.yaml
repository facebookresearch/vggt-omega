# @package _global_

defaults:
  - /experiments/cw/w009_save

submitit:
  name: "w009_save_no_conf"

trainer:
  optim:
    frozen_param_names:
      - "*depth_head.proj_conf*"

  loss:
    depth:
      use_conf_loss: False

    point:
      use_conf_loss: False

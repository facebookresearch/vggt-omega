# @package _group_
_target_: vggt.data.datasets.unified.UnifiedDataset
UNIFIED_DIR: s3://dino/datasets/repligen/unified/wai_structured3d/
dataset_prefix: "_wai_structured3d"
step_range: 6    # actually only 10 frames in each sequence
max_depth: 256
len_dataset: 273

len_dataset_scale: 0.2

is_synt: false
is_g: false

# @package _global_
#
# Add only atom on top of r001 and scale it to about 30% of train sampling.

defaults:
  - /experiments/cw/recheck/r001
  - /experiments/cw/datasets_synt/atom@trainer.data.train.dataset.dataset_configs.atom

submitit:
  name: "r007_atom"

trainer:
  data:
    train:
      dataset:
        dataset_configs:
          atom:
            len_dataset_scale: 29.72

# @package _global_
#
# Use 512 resolution on top of r001, and match w003_t4's
# dyna_ratio to keep the dynamic batch size within memory limits.

defaults:
  - /experiments/cw/recheck/r001

submitit:
  name: "r006"

img_size: 512
dyna_ratio: 0.5

# @package _group_
_target_: vggt.data.datasets.unified.UnifiedDataset
UNIFIED_DIR: /checkpoint/repligen/jianyuan/data/staticthings3d/
dataset_prefix: "_staticthings3d"
step_range: 64  # only 10 frames in each sequence, so this does not matter
max_depth: 128
len_dataset: 298

len_dataset_scale: 0.1

is_synt: True





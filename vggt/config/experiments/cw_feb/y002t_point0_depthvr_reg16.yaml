# @package _global_
# Ablation: point loss weight 0.0, depth valid_range -1, aggregator_register_num 16

defaults:
  - /experiments/cw/y002t_point0_depthvr

submitit:
  name: "y002t_point0_depthvr_reg16"

aggregator_register_num: 16

trainer:
  checkpoint:
    model_weight_initializer:
      state_dict:
        checkpoints:
          - checkpoint_path: /checkpoint/amaia/explore/jianyuan/vggt/config/experiments/cw/Full_1b_d015_5e5_t2.yaml/0/checkpoints/checkpoint_ab_e98.pt
            ckpt_state_dict_keys:
            - model
            checkpoint_kernels:
              - _target_: omnivision.model.checkpoint_utils.CkptExcludeKernel
                key_pattern:
                  - "*head*"
                  - "*aggregator.register_token*"


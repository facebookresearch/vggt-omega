# @package _global_
# Ablation 11: align_pred_to_gt: False & normalize_predictions: False

defaults:
  - /experiments/cw/reproduce_pi3

submitit:
  name: "ablation_11_no_align_no_norm"

trainer:
  loss:
    align_pred_to_gt: False
    normalize_predictions: False


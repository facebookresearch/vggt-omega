# @package _global_
# Ablation: conf_activation softplus without max clamp (softplus_0.5 instead of softplus_0.5_5)

defaults:
  - /experiments/cw/y005_n1_conf

trainer:
  model:
    DEPTH_HEAD:
      conf_activation: "softplus_0.5"
    POINT_HEAD:
      conf_activation: "softplus_0.5"


# @package _global_
# Ablation v2-11: aa_order: ["global", "frame"]

defaults:
  - /experiments/cw/reproduce_pi3_v2

submitit:
  name: "ab11"

trainer:
  model:
    AGGREGATOR:
      aa_order: ["global", "frame"]


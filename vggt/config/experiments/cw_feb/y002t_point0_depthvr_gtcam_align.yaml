# @package _global_
# Ablation: point loss weight 0.0, depth valid_range -1, normalization_type global_gtcam, align_pred_to_gt

defaults:
  - /experiments/cw/y002t_point0_depthvr_gtcam

submitit:
  name: "y002t_point0_depthvr_gtcam_align"

trainer:
  loss:
    align_pred_to_gt: True


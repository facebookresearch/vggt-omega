# @package _global_
# Ablation 4: use_dino_clsreg: True

defaults:
  - /experiments/cw/reproduce_pi3

submitit:
  name: "ablation_04_dino_clsreg"

trainer:
  model:
    AGGREGATOR:
      use_dino_clsreg: True


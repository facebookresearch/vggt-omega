# @package _global_
# Ablation: point loss weight 0.0, depth valid_range -1, normalization_type global_gtcam

defaults:
  - /experiments/cw/y002t_point0_depthvr

submitit:
  name: "y002t_point0_depthvr_gtcam"

trainer:
  loss:
    normalization_type: "global_gtcam"


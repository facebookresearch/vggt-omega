# @package _global_
# Ablation 22: aa_order: ["frame", "global"] (reversed order)

defaults:
  - /experiments/cw/reproduce_pi3

submitit:
  name: "ablation_22_aa_order_frame_global"

trainer:
  model:
    AGGREGATOR:
      aa_order: ["frame", "global"]


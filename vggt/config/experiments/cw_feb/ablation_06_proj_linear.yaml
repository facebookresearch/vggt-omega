# @package _global_
# Ablation 6: DEPTH_HEAD proj_type: "linear"

defaults:
  - /experiments/cw/reproduce_pi3

submitit:
  name: "ablation_06_proj_linear"

trainer:
  model:
    DEPTH_HEAD:
      proj_type: "linear"


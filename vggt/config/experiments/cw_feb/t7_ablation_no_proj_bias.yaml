# @package _global_
#
# Ablation from t7: Depth head proj_bias: False

defaults:
  - /experiments/cw/t7

submitit:
  name: "t7_ablation_no_proj_bias"

trainer:
  model:
    DEPTH_HEAD:
      proj_bias: False


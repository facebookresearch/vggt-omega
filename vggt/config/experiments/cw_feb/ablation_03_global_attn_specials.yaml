# @package _global_
# Ablation 3: AGGREGATOR global_attn_mode: "specials" & global_attn_partial_ratio: 0.25

defaults:
  - /experiments/cw/reproduce_pi3

submitit:
  name: "ablation_03_global_attn_specials"

trainer:
  model:
    AGGREGATOR:
      global_attn_mode: "specials"
      global_attn_partial_ratio: 0.25


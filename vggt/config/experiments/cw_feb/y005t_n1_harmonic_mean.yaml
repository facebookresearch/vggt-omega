# @package _global_
# Ablation: y005t with node=1, explore queue, use harmonic mean

defaults:
  - /experiments/cw/y005t_n1

submitit:
  name: "y005_n1_harmonic_mean"

trainer:
  loss:
    mean_type: "harmonic"


# @package _global_
# Curated ablation config (inherits z001_n1)

defaults:
  - /experiments/cw/z001_n1

submitit:
  name: "z001_n1_ablation_01_lap_grad_strong"

loss:
  depth:
    weight_normal: 0.35
    use_conf_loss: true
    use_gt_var_weighting: false
    gradient_loss_config:
      - type: "lap"
        weight: 0.1
        scales: 4
      - type: "grad"
        scales: 4

# @package _global_
# Ablation 8: DEPTH_HEAD disable_last_layer_amp: False

defaults:
  - /experiments/cw/reproduce_pi3

submitit:
  name: "ablation_08_enable_last_layer_amp"

trainer:
  model:
    DEPTH_HEAD:
      disable_last_layer_amp: False


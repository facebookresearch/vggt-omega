# @package _global_
# Ablation 16: skip_extreme_rescale: False & rescale_prob: -1

defaults:
  - /experiments/cw/reproduce_pi3

submitit:
  name: "ablation_16_no_skip_extreme"

trainer:
  data:
    train:
      common_config:
        skip_extreme_rescale: False
        rescale_prob: -1


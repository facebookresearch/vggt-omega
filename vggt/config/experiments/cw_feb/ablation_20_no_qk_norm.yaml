# @package _global_
# Ablation 20: use_qk_norm: False

defaults:
  - /experiments/cw/reproduce_pi3

submitit:
  name: "ablation_20_no_qk_norm"

trainer:
  model:
    AGGREGATOR:
      use_qk_norm: False


# @package _global_
#
# Ablation from t7: proj_zero_init: True & align_pred_to_gt: False

defaults:
  - /experiments/cw/t7

submitit:
  name: "t7_ablation_proj_zero_init_no_align"

trainer:
  model:
    DEPTH_HEAD:
      proj_zero_init: True

  loss:
    align_pred_to_gt: False


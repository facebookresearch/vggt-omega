# @package _global_
# Curated ablation config (inherits z001_n1)

defaults:
  - /experiments/cw/z001_n1

submitit:
  name: "z001_n1_ablation_27_grad_only_scales5"

loss:
  depth:
    weight_normal: 0.2
    use_conf_loss: true
    use_gt_var_weighting: false
    gradient_loss_config:
      - type: "grad"
        scales: 5

# @package _global_
# Ablation v2-15: AGGREGATOR global_attn_mode: "specials", global_attn_partial_ratio: 0.25

defaults:
  - /experiments/cw/reproduce_pi3_v2

submitit:
  name: "ab15"

trainer:
  model:
    AGGREGATOR:
      global_attn_mode: "specials"
      global_attn_partial_ratio: 0.25


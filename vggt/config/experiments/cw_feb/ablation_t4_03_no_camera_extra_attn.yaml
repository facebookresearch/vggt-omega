# @package _global_
# Ablation t4-03: No extra attention layers in camera head

defaults:
  - /experiments/cw/t4

submitit:
  name: "t4_ab03"

trainer:
  model:
    CAMERA_HEAD:
      extra_attention_depth: 0



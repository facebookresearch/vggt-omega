# @package _global_
# Ablation: conf_activation = expp1 (exp) instead of softplus

defaults:
  - /experiments/cw/y005_n1_conf

trainer:
  model:
    DEPTH_HEAD:
      conf_activation: "expp1"
    POINT_HEAD:
      conf_activation: "expp1"


# @package _global_
# Ablation 13: camera weight: 0.02

defaults:
  - /experiments/cw/reproduce_pi3

submitit:
  name: "ablation_13_camera_weight_002"

trainer:
  loss:
    camera:
      weight: 0.02


# @package _global_
# Ablation v2-21: DEPTH_HEAD disable_last_layer_amp: False

defaults:
  - /experiments/cw/reproduce_pi3_v2

submitit:
  name: "ab21"

trainer:
  model:
    DEPTH_HEAD:
      disable_last_layer_amp: False


# @package _global_
# Ablation: y005 with node=1, explore queue, use harmonic mean

defaults:
  - /experiments/cw/y005_n1

submitit:
  name: "y005_n1_harmonic_mean"

trainer:
  loss:
    mean_type: "harmonic"


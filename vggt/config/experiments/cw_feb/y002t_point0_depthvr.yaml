# @package _global_
# Ablation: point loss weight 0.0, depth valid_range -1

defaults:
  - /experiments/cw/y002t_point0

submitit:
  name: "y002t_point0_depthvr"

trainer:
  loss:
    depth:
      valid_range: -1


# @package _global_
# Ablation v2-12: DEPTH_HEAD output_dim: 2, activation: "exp"
# Test predicting depth directly (1 channel) instead of 3D local points (3 channels)

defaults:
  - /experiments/cw/reproduce_pi3_v2

submitit:
  name: "ab12"

trainer:
  model:
    DEPTH_HEAD:
      output_dim: 2
      activation: "exp"


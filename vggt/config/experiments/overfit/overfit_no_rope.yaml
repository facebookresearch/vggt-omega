# @package _global_
# Inherits from overfit_base.yaml with disable_rope_global: True

defaults:
  - /experiments/overfit/overfit_base
  - _self_

submitit:
  name: "overfit_no_rope"

trainer:
  model:
    AGGREGATOR:
      disable_rope_global: True


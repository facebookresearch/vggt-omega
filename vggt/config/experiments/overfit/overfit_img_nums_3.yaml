# @package _global_
# Inherits from overfit_base.yaml
# Changes img_nums from [1, 24] to [1, 3]

defaults:
  - /experiments/overfit/overfit_base

submitit:
  name: "overfit_img_nums_3"

trainer:
  data:
    train:
      common_config:
        img_nums: [1, 3]


# @package _global_
# Inherits from overfit_base.yaml without aggregator_layerwise_decay

defaults:
  - /experiments/overfit/overfit_base
  - _self_

submitit:
  name: "overfit_no_layerwise_decay"

trainer:
  optim:
    aggregator_layerwise_decay: null


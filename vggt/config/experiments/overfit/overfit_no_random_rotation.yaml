# @package _global_
# Inherits from overfit_base.yaml
# Changes random_rotation_ratio to -1 (disabled)

defaults:
  - /experiments/overfit/overfit_base

submitit:
  name: "overfit_no_random_rotation"

trainer:
  data:
    train:
      common_config:
        random_rotation_ratio: -1


# @package _global_
# Inherits from overfit_base.yaml
# Disables skip_extreme_rescale

defaults:
  - /experiments/overfit/overfit_base

submitit:
  name: "overfit_no_skip_extreme_rescale"

trainer:
  data:
    train:
      common_config:
        skip_extreme_rescale: False


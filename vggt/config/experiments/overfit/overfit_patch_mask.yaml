# @package _global_
# Inherits from overfit_base.yaml
# Changes random_patch_mask_ratio to 0.05

defaults:
  - /experiments/overfit/overfit_base

submitit:
  name: "overfit_patch_mask"

trainer:
  data:
    train:
      common_config:
        random_patch_mask_ratio: 0.05


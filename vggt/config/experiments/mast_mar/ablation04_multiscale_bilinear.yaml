# @package _global_
#

defaults:
  - /experiments/mast/ablation01_multiscale_deconv_concat
  - _self_


submitit:
  name: "ab04"

trainer:
  model:
    DEPTH_HEAD:
      # Ablation 5: interpolate-only upsample to 1/4.
      multiscale_resize_type: "bilinear"


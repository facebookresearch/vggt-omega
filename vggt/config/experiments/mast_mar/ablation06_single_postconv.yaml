# @package _global_
#

defaults:
  - /experiments/mast/ablation00_single_lastlayer
  - _self_


submitit:
  name: "ab06"

trainer:
  model:
    DEPTH_HEAD:
      # Ablation 7: single-level legacy path + an extra post conv.
      post_conv_type: "conv"


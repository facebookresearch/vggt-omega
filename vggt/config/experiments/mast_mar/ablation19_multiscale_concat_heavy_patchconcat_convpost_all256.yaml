# @package _global_
#

defaults:
  - /experiments/mast/ablation18_multiscale_concat_heavy_patchconcat_convpost
  - _self_


submitit:
  name: "ab19"

trainer:
  model:
    DEPTH_HEAD:
      # Keep ab18 design, but unify all branch output channels to 256.
      multiscale_out_channels: [256, 256, 256, 256]
      patch_embed_out_channels: 256


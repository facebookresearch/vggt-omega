# @package _global_
#

defaults:
  - /experiments/mast/ablation07_multiscale_bilinear_conv_256
  - _self_


submitit:
  name: "ab08"

trainer:
  model:
    DEPTH_HEAD:
      # Same as ablation07_multiscale_bilinear_conv_256, but merge multi-scale features by add instead of concat.
      multiscale_merge_type: "add_inplace"


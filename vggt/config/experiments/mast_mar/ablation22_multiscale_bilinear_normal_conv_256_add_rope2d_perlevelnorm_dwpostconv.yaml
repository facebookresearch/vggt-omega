# @package _global_
#

defaults:
  - /experiments/mast/ablation17_multiscale_bilinear_dw_256_add_rope2d_perlevelnorm_dwpostconv
  - _self_


submitit:
  name: "ab22"

trainer:
  model:
    DEPTH_HEAD:
      # Keep ab17 settings and switch resize op to bilinear + normal conv.
      multiscale_resize_type: "bilinear_normal_conv"



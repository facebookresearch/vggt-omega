# @package _global_
#

defaults:
  - /experiments/mast/ablation04_multiscale_bilinear
  - _self_


submitit:
  name: "ab09"

trainer:
  model:
    DEPTH_HEAD:
      # Pure bilinear + 256 channels per level + add merge.
      multiscale_out_channels: [256, 256, 256, 256]
      multiscale_merge_type: "add_inplace"


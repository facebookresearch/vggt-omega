# @package _global_
#

defaults:
  - /experiments/mast/ablation01_multiscale_deconv_concat
  - _self_


submitit:
  name: "ab03"

trainer:
  model:
    DEPTH_HEAD:
      # Ablation 4: interpolate + normal conv upsample to 1/4.
      multiscale_resize_type: "bilinear_normal_conv"


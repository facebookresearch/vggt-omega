# @package _global_
#

defaults:
  - /experiments/mast/ablation17_multiscale_bilinear_dw_256_add_rope2d_perlevelnorm_dwpostconv
  - _self_


submitit:
  name: "ab20"

trainer:
  model:
    DEPTH_HEAD:
      # Keep ab17 settings and only switch multiscale fusion from add to concat.
      multiscale_merge_type: "concat"



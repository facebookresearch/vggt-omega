# @package _global_
#

defaults:
  - /experiments/mast/ablation13_multiscale_bilinear_256_add_patchconcat
  - _self_


submitit:
  name: "ab14"

trainer:
  model:
    DEPTH_HEAD:
      # Patch-embed fusion but keep memory lower with add merge.
      patch_embed_merge_type: "add_inplace"
      patch_embed_out_channels: 256

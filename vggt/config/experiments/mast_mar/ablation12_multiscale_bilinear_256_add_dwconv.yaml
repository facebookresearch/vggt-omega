# @package _global_
#

defaults:
  - /experiments/mast/ablation09_multiscale_bilinear_256_add
  - _self_


submitit:
  name: "ab12"

trainer:
  model:
    DEPTH_HEAD:
      # Add a lightweight depthwise conv per multiscale head for local smoothing.
      multiscale_head_conv_type: "dwconv"

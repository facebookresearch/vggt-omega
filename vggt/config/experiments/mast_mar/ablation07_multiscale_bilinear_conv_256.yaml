# @package _global_
#

defaults:
  - /experiments/mast/ablation01_multiscale_deconv_concat
  - _self_


submitit:
  name: "ab07"

trainer:
  model:
    DEPTH_HEAD:
      # New ablation: 4-level features, interpolate + conv upsample to 1/4, 256 channels per level.
      intermediate_layer_idx: [4, 11, 17, 23]
      multiscale_out_channels: [256, 256, 256, 256]
      multiscale_upsample_factor: 4
      multiscale_resize_type: "bilinear_normal_conv"

